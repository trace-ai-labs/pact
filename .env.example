# Baseten Model APIs (the default endpoint).
BASETEN_API_KEY=

# Hugging Face token, for the gated dataset. Accept the license at
# https://huggingface.co/datasets/trace-ai-labs/pact first.
# `huggingface-cli login` works instead of setting this.
HF_TOKEN=

# Optional: point at any other OpenAI-compatible provider.
# PACT_BASE_URL=https://your-provider.example/v1
# PACT_API_KEY_ENV=YOUR_PROVIDER_KEY
